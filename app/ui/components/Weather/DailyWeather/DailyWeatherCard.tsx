'use client';
interface DailyWeatherProps {
  hourlyData: Array<string>;
  dailyData: Array<number>;
}
export default function DailyWeatherCard({
  hourlyData,
  dailyData,
}: DailyWeatherProps) {
  return <div>
    
  </div>;
}
